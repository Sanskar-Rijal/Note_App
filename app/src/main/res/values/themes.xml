<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Note" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- making seperate theme for splash screen -->
    <style name="Theme.Splash_Screen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Note</item>

    </style>
</resources>